# indicator4cj 代码风格完善计划 v3.0

## 📋 计划概述

**版本**: v3.0
**创建日期**: 2026-01-15
**基于**: 像素级代码风格对比审核结果
**目标**: 100% 符合仓颉官方项目标准（feign4cj, deque4cj）

---

## 🔍 审核发现

### 当前符合度：**65%**

| 检查项 | 符合度 | 优先级 | 影响范围 |
|--------|--------|--------|----------|
| 文件头部结构 | ✅ 100% | - | - |
| 花括号位置 | ✅ 100% | - | - |
| 运算符空格 | ✅ 100% | - | - |
| 类型注解空格 | ✅ 100% | - | - |
| 字段初始化 | ❌ 0% | 🔴 P0 | 所有类 |
| 方法注释 | ❌ 10% | 🔴 P0 | 所有 public 方法 |
| 构造函数格式 | ⚠️ 60% | 🟡 P1 | 部分类 |
| 字段注释 | ❌ 5% | 🟢 P2 | 关键字段 |
| 数组索引 | ✅ 100% | - | - |
| match 表达式 | ✅ 100% | - | - |
| 字符串插值 | ✅ 100% | - | - |

---

## 🎯 修复目标

### 核心目标（P0 - 必须修复）

#### 1. 字段初始化风格 ❌→✅

**参考标准** (deque4cj):
```cj
public var _buf: Array<?TElement> = Array<?TElement>()
public var _head: Int64 = 0
public var _count: Int64 = 0
```

**当前代码**:
```cj
public class Ema {
    public var period: Int64          // ❌ 没有默认值
    public var smoothing: Float64      // ❌ 没有默认值
}
```

**修复后**:
```cj
public class Ema {
    public var period: Int64 = 20          // ✅ 有默认值
    public var smoothing: Float64 = 2.0    // ✅ 有默认值
}
```

**规则**:
- 所有 `public var` 字段都必须在定义时初始化
- `private let` 字段可以在构造函数中初始化
- 数值类型：`Int64 = 0`, `Float64 = 0.0`
- 布尔类型：`Bool = false`
- 对象类型：使用默认构造函数

#### 2. 方法注释风格 ❌→✅

**参考标准** (deque4cj):
```cj
// cap 返回 Deque 的当前容量。
public func cap(): Int64 {
    return _buf.size
}

// len 返回队列中当前存储的元素数量。
public func len(): Int64 {
    return _count
}
```

**当前代码**:
```cj
public func compute(c: Iterator<Float64>): Iterator<Float64> {  // ❌ 无注释
    return EmaIterator(c, period, smoothing)
}
```

**修复后**:
```cj
// compute 计算指数移动平均线。
public func compute(c: Iterator<Float64>): Iterator<Float64> {
    return EmaIterator(c, period, smoothing)
}

// idlePeriod 返回空闲周期数。
public func idlePeriod(): Int64 {
    return period - 1
}
```

**规则**:
- 所有 `public func` 必须有中文注释
- 注释格式：`// 方法名 方法描述。`
- 注释位置：方法定义上一行
- 私有方法可以不加注释（除非特别复杂）

---

### 次要目标（P1 - 建议修复）

#### 3. 构造函数格式统一 ⚠️→✅

**参考标准** (feign4cj):
```cj
public DefaultClient() {
    this.client = ClientBuilder().build()
}
```

**当前代码**:
```cj
public Sma() { this.period = 50 }  // ❌ 单行格式
public init(period: Int64) { this.period = period }  // ❌ 单行格式
```

**修复后**:
```cj
public Sma() {
    this.period = 50
}

public init(period: Int64) {
    this.period = period
}
```

**规则**:
- 构造函数体应该多行格式
- 即使只有一行代码，也应该换行
- 花括号单独一行（对于多行函数）

---

### 可选目标（P2 - 锦上添花）

#### 4. 字段注释添加

**参考标准** (deque4cj):
```cj
// 队列
public var _buf: Array<?TElement> = Array<?TElement>()
// 队列头部索引
public var _head: Int64 = 0
```

**建议**:
- 为关键字段添加简短注释
- 格式：`// 字段说明`
- 位置：字段定义上一行

---

## 📅 执行计划

### 阶段 1: 字段初始化修复 (P0) ⭐⭐⭐⭐⭐

**时间估算**: 1-2 天
**文件数**: ~60 个
**影响范围**: 所有 Indicator 类、Strategy 类、Asset 类

#### 任务清单

##### 1.1 Trend 模块字段初始化
**文件**: 24 个指标类

**修复模式**:
```cj
// ❌ 修复前
public class Sma {
    public var period: Int64
    public Sma() { this.period = 50 }
}

// ✅ 修复后
public class Sma {
    public var period: Int64 = 50
    public Sma() { this.period = period }
}
```

**注意**:
- 字段初始化值应与无参构造函数的默认值一致
- 需要同步更新构造函数

**文件列表**:
- `trend/sma.cj`
- `trend/ema.cj`
- `trend/wma.cj`
- `trend/dema.cj`
- `trend/tema.cj`
- `trend/trima.cj`
- `trend/macd.cj`
- `trend/apo.cj`
- 等等...

##### 1.2 Momentum 模块字段初始化
**文件**: 9 个指标类

##### 1.3 Volatility 模块字段初始化
**文件**: 12 个指标类

##### 1.4 Volume 模块字段初始化
**文件**: 11 个指标类

##### 1.5 Strategy 和 Asset 模块字段初始化
**文件**: ~10 个

#### 测试策略
```bash
# 修复每个模块后测试
cjpm test -- test/trend/*
cjpm test -- test/momentum/*
cjpm test -- test/volatility/*
cjpm test -- test/volume/*
```

---

### 阶段 2: 方法注释添加 (P0) ⭐⭐⭐⭐⭐

**时间估算**: 2-3 天
**文件数**: ~60 个
**新增注释**: ~200 条

#### 任务清单

##### 2.1 Indicator 类方法注释

**需要注释的方法**:
- `compute()` - 计算[指标名]值
- `idlePeriod()` - 返回空闲周期数
- `toString()` - 返回指标的字符串表示

**示例**:
```cj
// Simple Moving Average (Float64).
public class Sma {
    public var period: Int64 = 50

    // compute 计算简单移动平均线。
    public func compute(c: Iterator<Float64>): Iterator<Float64> {
        let sum = MovingSum()
        sum.period = period
        return DivideBy(sum.compute(c), Float64(period))
    }

    // idlePeriod 返回空闲周期数。
    public func idlePeriod(): Int64 {
        return period - 1
    }

    // toString 返回指标的字符串表示。
    public func toString(): String {
        return "SMA(${period})"
    }
}
```

##### 2.2 Strategy 类方法注释

**需要注释的方法**:
- `name()` - 返回策略名称
- `compute()` - 生成交易动作
- `report()` - 生成策略报告

**示例**:
```cj
// Buy on first tick, hold thereafter.
public class BuyAndHoldStrategy <: Strategy {
    private var first: Bool = true

    // name 返回策略名称。
    public func name(): String {
        return "BuyAndHold"
    }

    // compute 生成交易动作流。
    public func compute(snapshots: Iterator<Snapshot>): Iterator<Action> {
        return BuyAndHoldIterator(snapshots)
    }

    // report 生成策略报告。
    public func report(snapshots: Iterator<Snapshot>): Report {
        let dates = SnapshotsAsDates(snapshots)
        return Report(name(), dates)
    }
}
```

##### 2.3 Asset 和 Helper 类方法注释

**需要注释的类**:
- Repository 接口和实现
- Csv 类
- Report 类

#### 注释规范

**格式要求**:
```cj
// methodName 方法描述。
public func methodName(param: Type): ReturnType {
    // 实现
}
```

**注意事项**:
- 注释使用中文
- 简洁明了，一句话说明功能
- 以句号结尾
- 不写参数说明（除非特殊情况）

---

### 阶段 3: 构造函数格式统一 (P1) ⭐⭐⭐

**时间估算**: 1 天
**文件数**: ~40 个

#### 任务清单

##### 3.1 单行构造函数改为多行

**修复模式**:
```cj
// ❌ 修复前
public Sma() { this.period = 50 }
public init(period: Int64) { this.period = period }

// ✅ 修复后
public Sma() {
    this.period = 50
}

public init(period: Int64) {
    this.period = period
}
```

**受影响文件**:
- 所有使用单行构造函数的类
- 约 40 个文件

---

### 阶段 4: 字段注释添加 (P2) ⭐⭐

**时间估算**: 1 天
**文件数**: ~60 个（可选）

#### 任务清单

##### 4.1 为关键字段添加注释

**示例**:
```cj
public class Ema {
    // 周期
    public var period: Int64 = 20
    // 平滑系数
    public var smoothing: Float64 = 2.0
}
```

**需要添加注释的字段**:
- 所有 public 字段
- 特别是：period, smoothing, multiplier, fastPeriod, slowPeriod 等

---

## 📝 执行检查清单

### 每个文件修改前
- [ ] 运行相关测试建立基线
- [ ] 备份当前文件（Git commit）

### 每个文件修改中
- [ ] 字段初始化：添加默认值
- [ ] 方法注释：添加中文注释
- [ ] 构造函数格式：统一为多行
- [ ] 字段注释：添加关键字段注释（可选）

### 每个文件修改后
- [ ] 编译通过：`cjpm build`
- [ ] 测试通过：`cjpm test -- path/to/test.cj`
- [ ] Git commit：单个文件或单模块 commit
- [ ] 更新进度日志

### 每个阶段结束时
- [ ] 完整测试：`cjpm test` (240/240)
- [ ] 代码审查：抽查 20% 文件
- [ ] Git tag：标记阶段完成
- [ ] 推送到远程：`git push origin dev`

---

## 📊 进度跟踪

### 阶段完成统计

| 阶段 | 模块 | 状态 | 完成日期 | Commit | 测试通过 |
|------|------|------|---------|--------|---------|
| 1 | 字段初始化 (P0) | ⏳ 待开始 | - | - | - |
| 2 | 方法注释 (P0) | ⏳ 待开始 | - | - | - |
| 3 | 构造函数格式 (P1) | ⏳ 待开始 | - | - | - |
| 4 | 字段注释 (P2) | ⏳ 待开始 | - | - | - |

### 问题跟踪

| 问题描述 | 文件 | 行号 | 状态 | 解决方案 |
|---------|------|------|------|---------|
| - | - | - | - | - |

---

## ⚠️ 风险管理

### 风险 1: 字段初始化值冲突
**描述**: 字段初始化值可能与构造函数中的设置不一致
**概率**: 中
**影响**: 高
**应对**:
- 初始化值应与无参构造函数的默认值一致
- 修改字段初始化后，构造函数改为 `this.field = field`
- 仔细测试每个类的构造逻辑

### 风险 2: 大规模修改导致测试失败
**描述**: 240 个测试可能因修改失败
**概率**: 中
**影响**: 高
**应对**:
- 分阶段修改和测试
- 每个模块独立验证
- 保留详细 Commit 历史，方便回滚

### 风险 3: 注释质量问题
**描述**: 中文注释质量可能不一致
**概率**: 低
**影响**: 低
**应对**:
- 建立注释模板
- 审查关键类的注释
- 必要时进行第二轮优化

---

## 🎯 验收标准

### 最终验收（全部阶段完成）

1. ✅ **字段初始化**: 100% 的字段在定义时初始化
2. ✅ **方法注释**: 100% 的 public 方法有中文注释
3. ✅ **构造函数格式**: 100% 使用多行格式
4. ✅ **字段注释**: 关键字段有中文注释（可选）

### 测试验收

```bash
✓ 编译验证: cjpm build success
✓ 测试验证: 240/240 passed
✓ 代码质量: 符合 deque4cj 和 feign4cj 标准
```

### 代码风格验收

**对比参考项目**:
- deque4cj/src/deque4cj.cj
- feign4cj/src/core/client.cj

**符合度目标**: **95%+** （当前 65% → 目标 95%）

---

## 📅 时间估算（更新版）

| 阶段 | 模块 | 文件数 | 预估天数 | 累计天数 |
|------|------|--------|----------|----------|
| 1 | 字段初始化 (P0) | ~60 | 1-2 | 1-2 |
| 2 | 方法注释 (P0) | ~60 | 2-3 | 3-5 |
| 3 | 构造函数格式 (P1) | ~40 | 1 | 4-6 |
| 4 | 字段注释 (P2) | ~60 | 1 | 5-7 |

**总时间估算**: 5-7 天

---

## 🔧 快速参考

### 修改模式速查

| 模式 | 查找 | 替换为 |
|------|------|--------|
| 字段初始化 | `public var name: Type` | `public var name: Type = defaultValue` |
| 方法注释 | `public func name(` | `// name 描述。\npublic func name(` |
| 构造函数 | `public Name() { }` | `public Name() {\n    ...\n}` |
| 字段注释 | `public var name: Type` | `// 描述\npublic var name: Type` |

### 默认值参考

| 类型 | 默认值 |
|------|--------|
| `Int64` | `0` |
| `Float64` | `0.0` |
| `Bool` | `false` |
| `String` | `""` |
| `Array<T>` | `Array<T>()` |
| `ArrayList<T>` | `ArrayList<T>()` |

### 注释模板

```cj
// compute 计算[指标名]。
// idlePeriod 返回空闲周期数。
// toString 返回指标的字符串表示。
// name 返回策略名称。
// compute 生成交易动作流。
// report 生成策略报告。
```

---

**最后更新**: 2026-01-15 v3.0
**计划制定者**: Claude (Sonnet 4.5)
**参考标准**: I:\indicator4cj\completed_projects
**审核依据**: 像素级代码风格对比审核报告
